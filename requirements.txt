# ============================================================
# AI-WhaleWatcher 依赖
# Python 3.12
# ============================================================

# --- 数据源 ---
# XTick HTTP API (主力数据源)
# 注册获取 token: http://www.xtick.top
requests>=2.31
# AkShare / 东方财富 (免费备用数据源)
akshare   #>=1.14.0

# --- 数据库 ---
sqlalchemy>=2.0
pymysql>=1.1
redis>=5.0
# 可选：ClickHouse 时序存储
# clickhouse-driver>=0.2

# --- 任务调度 ---
celery>=5.4
# APScheduler（轻量原型阶段可用）
apscheduler>=3.10

# --- 配置 ---
pyyaml>=6.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# --- AI / LLM ---
openai>=1.0
anthropic>=0.30

# --- 日志 ---
loguru>=0.7

# --- 测试 ---
pytest>=8.0
pytest-cov>=5.0
